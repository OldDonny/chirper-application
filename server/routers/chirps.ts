
import {Router, Response, Request, NextFunction} from 'express';

import randomId from '../middleware/id';
// import { all, update, create, read, destroy } from '../procedures/chirps.proc';


const router= Router();

// router

// .get('/', all)

// .post('/', randomId, create)

// .put('/' , update)

// .get(`/:id`, read)

// .delete(`/:id`, destroy);

export default router;